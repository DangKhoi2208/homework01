let array = [];
let x = prompt();
for(let i = 0 ; i <  x ; i++){
    let temp = [];
    
    for(let y = 0 ; y <5 ; y ++ ){
        temp.push(randomNumber(27,13))
        // console.table(temp);
    }
    

    array.push(temp)
}
 function randomNumber(a , b){

    return Math.floor(Math.random()* (a - b + 1) + b )
 }
console.table(array);
for(let x = 0 ; x < n ; x++)
{
    for(let y = 0 ;y < n ; y++)
{
    let sum = 0;
    if(i - 1 >= 0)
    {
        sum += arr[i-1][j] 
    }
}
}

